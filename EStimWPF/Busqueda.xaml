<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EStimWPF"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" x:Class="EStimWPF.Busqueda"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Busqueda">
    <Grid>
        <ListView Name="listaJuegos">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border  BorderBrush="Black" BorderThickness="4">
                        <WrapPanel Background="Purple" >
                            <Image Source="/EStimWPF;component/testing/tekken8.jpg" Width="100" Height="100"/>
                            <TextBlock Text="{Binding Nombre}" Margin="0,20,10,0"/>
                            <TextBlock Text="{Binding Desarrollador}" Margin="0,20,10,0"/>
                        </WrapPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ui:Button Content="Button" Margin="550,332,0,0" VerticalAlignment="Top" Click="CambiarNombre"/>
    </Grid>
</Page>
